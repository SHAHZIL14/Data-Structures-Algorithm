DATA STRUCTURE:-> Structures that stores data.

ARRAY :-> Block of data.
->Store same data type for ex-> int , float and other.
->Contigous Memory (linear).
->

Syntax:->
'data type' 'name' ['size'];

Accessing data:->
1. Index -> Starts with zero;

for example array[0]-> value at zeroth index of array.

2. Size of array-> SIZE variable or sizeof(array)/sizeof(type).

3. 

